import { ModerationInterceptor } from './moderation.interceptor';

describe('ModerationInterceptor', () => {
  it('should be defined', () => {
    expect(new ModerationInterceptor()).toBeDefined();
  });
});
