mutation editHub(
    $hubId: ID!
    $name: String!
    $description: String!
    ) {
    editHub(
        hubId: $hubId
        name: $name
        description: $description
        ) {
        id
        name
        description
    }
}