mutation resetPassword(
    $email: String!
    $newPassword: String!
    $resetPin: String!
) {
    resetPassword(
    usersEmail: $email,
    newPassword: $newPassword,
    resetPin: $resetPin
    )
}