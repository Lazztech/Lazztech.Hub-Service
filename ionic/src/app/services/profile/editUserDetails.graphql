mutation editUserDetails(
    $firstName: String!
    $lastName: String!
    $description: String!
    ) {
        editUserDetails(
            firstName: $firstName, 
            lastName: $lastName, 
            description: $description
            ) {
        id
        firstName
        lastName
        description
        }
}