overwrite: true
schema: "../back-end/schema.gql"
# documents: "src/**/*.{ts,tsx}"
# config:
#   gqlImport: graphql.macro#gql
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-apollo-angular"
  ./graphql.schema.json:
    plugins:
      - "introspection"
